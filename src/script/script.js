const btnClick = document.getElementById("btn");

/*const inputs = document.getElementsByTagName("input");

const usuario = inputs[0];

const senha = inputs[1];*/

btnClick.addEventListener('click', function () {
    alert("Desculpe! o sistema ainda está em desenvolvimento.")

});

/*btnClick.addEventListener('click', function() {

    if(usuario.value == '' || senha.value === '') {

        return alert('Por favor! preencha os campos de usuário e senha, obrigado!');
    }

    const mensagem = (`Seu usuário é: ${usuario.value} \nSua senha é: ${senha.value}.`);

    alert(mensagem);

});*/